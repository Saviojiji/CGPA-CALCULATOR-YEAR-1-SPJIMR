import { defineConfig } from 'vite';

export default defineConfig({
  // Base path for GitHub Pages — update to match your repo name
  base: '/CGPA-CALCULATOR-YEAR-1-SPJIMR
/',
  root: '.',
  build: {
    outDir: 'dist',
  },
});
