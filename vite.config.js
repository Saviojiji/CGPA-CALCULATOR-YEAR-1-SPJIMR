import { defineConfig } from 'vite';

export default defineConfig({
  // Base path for GitHub Pages — update to match your repo name
  base: '/SPJIMR-YEAR-CGPA-CALCULATOR/',
  root: '.',
  build: {
    outDir: 'dist',
  },
});
